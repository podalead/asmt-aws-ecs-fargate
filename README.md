**_faragte_** - simple example for non kubernetes microservices with healthcheck and load balancing

**_ec2_**     - example with ec2 instances as proxy & app holder, also added load balancing and elastic ip